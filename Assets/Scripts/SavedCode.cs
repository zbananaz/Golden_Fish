using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class SavedCode : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //List<Transform> check;
    //Transform nearest;
    //protected virtual Transform FindTarget()
    //{
    //    enemyColliders = Physics2D.OverlapCircleAll(transform.position, 13f, LayerMask.GetMask("Enemy"));

    //    check = enemyColliders.Select(x => x.transform).ToList();
    //    nearest = check.First();
    //    check.ForEach(x =>
    //    {
    //        if ((transform.position - x.transform.position).sqrMagnitude < (transform.position - nearest.transform.position).sqrMagnitude)
    //        {
    //            nearest = x;
    //            enemy = x.gameObject;
    //        }
    //    });
    //    return nearest;
    //}
}
